---
phase: 10-integration-and-end-to-end-validation
task: e2e-perf-optimization
total_tasks: 5
status: completed
last_updated: '2026-03-19T23:15:00.000Z'
---

<current_state>
E2e performance optimization COMPLETE. Total e2e time reduced from ~8m13s to ~2m07s (4x speedup).

Three bugs fixed:

1. Cached JSON fast path regex never matched pretty-printed output
2. Cached JSON fast path schema validation double-unwrapped envelope
3. Docker commit replaced with docker build (containerd snapshotter v1 47s floor)
   </current_state>

<completed_work>

## All previous work (DONE)

- fileMap guard fix, e2e infra improvements, research (see git history)

## Docker commit elimination (DONE)

- Root cause: containerd snapshotter v1 walks entire 14GB filesystem for every commit (47s floor for empty diff)
- Solution: Dockerfile.snapshot builds snapshot as thin layer on base image via docker build --network=host
- Verdaccio install works via --network=host (1.8s local roundtrip)
- No more node_modules deletion needed (no docker commit = no writable layer concern)
- startContainer no longer needs npm install restore (node_modules in image layer)

## Cached JSON fast path fix (DONE)

- Bug 1: indexOf('{"graph"') never matched pretty-printed `{\n  "graph"` — fixed with /\{\s\*"graph"/ regex
- Bug 2: safeParse(parsed.graph) double-unwrapped envelope — fixed to safeParse(fullParsedObject)
- Graph warming: ~5min -> 3.2s

## Dissection findings (DONE)

- Empty container commit: 47s (pure filesystem walk, irreducible)
- 40-entry diff commit: 1m49s
- Overlay2 opaque whiteout: rm -rf directory creates single D entry, not per-file entries
- Storage driver: overlayfs with io.containerd.snapshotter.v1 (not legacy overlay2)
  </completed_work>

<decisions_made>

- Dockerfile-based snapshot replaces runtime docker commit
- --network=host allows Verdaccio install during docker build (local roundtrip is fast)
- Verdaccio retained for publish/install fidelity
- node_modules stays in snapshot (no deletion needed without docker commit)
- startContainer simplified (no npm install restore)
  </decisions_made>
