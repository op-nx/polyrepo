---
phase: 05-avoid-type-casting-and-prefer-satisfies
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/op-nx-polyrepo/src/lib/config/resolve.ts
  - packages/op-nx-polyrepo/src/lib/graph/extract.ts
  - packages/op-nx-polyrepo/src/lib/executors/sync/executor.ts
  - packages/op-nx-polyrepo/src/lib/graph/types.ts
autonomous: true
requirements:
  - SAFE-ZOD

must_haves:
  truths:
    - "All 3 JSON.parse sites use Zod safeParse for runtime validation"
    - "No type annotations on JSON.parse results -- types flow from Zod schemas"
  artifacts:
    - path: "packages/op-nx-polyrepo/src/lib/config/resolve.ts"
      provides: "Zod-validated nx.json parsing"
      contains: "safeParse"
    - path: "packages/op-nx-polyrepo/src/lib/graph/extract.ts"
      provides: "Zod-validated graph JSON parsing"
      contains: "safeParse"
    - path: "packages/op-nx-polyrepo/src/lib/executors/sync/executor.ts"
      provides: "Zod-validated package.json parsing"
      contains: "safeParse"
  key_links:
    - from: "packages/op-nx-polyrepo/src/lib/config/resolve.ts"
      to: "zod"
      via: "schema.safeParse(JSON.parse(...))"
      pattern: "safeParse"
    - from: "packages/op-nx-polyrepo/src/lib/graph/extract.ts"
      to: "zod"
      via: "schema.safeParse(JSON.parse(...))"
      pattern: "safeParse"
    - from: "packages/op-nx-polyrepo/src/lib/executors/sync/executor.ts"
      to: "zod"
      via: "packageManagerSchema.safeParse(JSON.parse(...))"
      pattern: "safeParse"
---

<objective>
Add Zod validation at all 3 JSON.parse system boundaries. This is a focused plan addressing only the Zod boundary hardening -- production code lint/typecheck violations are handled separately in Plan 03.

Purpose: Every JSON.parse call must be validated at runtime, not just typed by annotation. Zod safeParse ensures invalid external data is caught immediately with descriptive errors.
Output: 3 files with Zod-validated JSON.parse calls, types flowing from schemas.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-RESEARCH.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-01-SUMMARY.md

@packages/op-nx-polyrepo/src/lib/config/resolve.ts
@packages/op-nx-polyrepo/src/lib/graph/extract.ts
@packages/op-nx-polyrepo/src/lib/graph/types.ts
@packages/op-nx-polyrepo/src/lib/executors/sync/executor.ts
@packages/op-nx-polyrepo/src/lib/config/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Zod schemas at all 3 JSON.parse system boundaries</name>
  <files>
    packages/op-nx-polyrepo/src/lib/config/resolve.ts,
    packages/op-nx-polyrepo/src/lib/graph/extract.ts,
    packages/op-nx-polyrepo/src/lib/executors/sync/executor.ts,
    packages/op-nx-polyrepo/src/lib/graph/types.ts
  </files>
  <action>
Replace all 3 JSON.parse type annotations with Zod safeParse validation. Follow the pattern from 05-RESEARCH.md "Zod Validation at System Boundaries" section.

**Site 1: resolve.ts line 17** -- `JSON.parse(readFileSync(nxJsonPath))` typed as `NxJsonConfiguration`
- Create a minimal Zod schema for the plugin-relevant subset of NxJsonConfiguration. Only need: `plugins` array where each element is either a string or an object with `plugin` (string) and optional `options` (unknown). Use `z.passthrough()` on the outer object since nx.json has many fields we don't need.
- Replace the type annotation with `safeParse`. On failure, throw a descriptive error mentioning the nx.json path.
- The schema can be defined at module level in resolve.ts (it's small enough to not warrant a separate file).

**Site 2: extract.ts line 64** -- `JSON.parse(jsonPayload)` typed as `ExternalGraphJson`
- Create a Zod schema matching the `ExternalGraphJson` interface from types.ts. See 05-RESEARCH.md "Zod Schema for JSON.parse Validation" code example for the exact schema shape.
- Place the schema in types.ts alongside the interface. Consider using `z.infer<typeof schema>` to derive the interface from the schema (single source of truth per user decision). If the existing interface has many consumers, keep the interface and validate against it.
- Replace the type annotation with safeParse. On failure, throw with repo path context.

**Site 3: executor.ts (sync) line 51** -- `JSON.parse(readFileSync(pkgJsonPath))` for package.json
- Create a minimal schema: `z.object({ packageManager: z.string().optional() }).passthrough()` -- we only need the packageManager field.
- Use safeParse with fallback (return undefined for packageManager if parse fails, since package.json should always be valid JSON but we want safety).

For all sites: import from `zod` (already a production dependency). Use `z.safeParse()` pattern. Prefer Zod-inferred types (`z.infer<typeof schema>`) where it simplifies the code per user decision.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['src/lib/config/resolve.ts','src/lib/graph/extract.ts','src/lib/executors/sync/executor.ts'].forEach(f => { const c=fs.readFileSync('packages/op-nx-polyrepo/'+f,'utf8'); if(!c.includes('safeParse')) throw new Error(f+' missing safeParse') }); console.log('[OK] all 3 JSON.parse sites use safeParse')"</automated>
  </verify>
  <done>All 3 JSON.parse sites validated via Zod safeParse. No type annotations on JSON.parse results -- types flow from Zod schemas.</done>
</task>

</tasks>

<verification>
- `git grep "JSON.parse" -- packages/op-nx-polyrepo/src/lib/ | rg -v "spec\.ts"` -- every JSON.parse site is followed by safeParse usage
- `npm exec nx typecheck @op-nx/polyrepo 2>&1 | tail -5` -- Zod schema types are compatible (may still have other violations from strict rules)
</verification>

<success_criteria>
- All 3 JSON.parse sites use Zod safeParse
- Types flow from Zod schemas (z.infer where appropriate)
- Descriptive error messages on parse failure
- NOTE: Production code may still have other lint/typecheck violations -- Plan 03 handles those
</success_criteria>

<output>
After completion, create `.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-02-SUMMARY.md`
</output>
