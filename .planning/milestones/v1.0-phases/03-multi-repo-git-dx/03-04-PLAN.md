---
phase: 03-multi-repo-git-dx
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/op-nx-polyrepo/src/lib/executors/status/executor.ts
  - packages/op-nx-polyrepo/src/lib/executors/status/executor.spec.ts
autonomous: true
requirements: [GITX-01, GITX-03]
gap_closure: true

must_haves:
  truths:
    - "Summary line includes count of repos behind and repos ahead (e.g. '1 behind, 2 ahead')"
    - "Clean repos show 'ok' instead of 'clean' in dirty summary column"
    - 'Tag-pinned repos show [WARN: tag-pinned] warning'
  artifacts:
    - path: 'packages/op-nx-polyrepo/src/lib/executors/status/executor.ts'
      provides: 'Enhanced summary line with behind/ahead counts, tag-pinned warning, descriptive dirty label'
    - path: 'packages/op-nx-polyrepo/src/lib/executors/status/executor.spec.ts'
      provides: 'Tests for new summary counts, ok label, tag-pinned warning'
  key_links:
    - from: 'RepoRowData collection loop'
      to: 'summary line logger.info'
      via: 'raw AheadBehind retained and aggregated across repos'
      pattern: 'reposBehind|reposAhead'
    - from: 'isTagPinned check'
      to: 'warnings array push'
      via: 'new tag-pinned warning branch'
      pattern: 'WARN: tag-pinned'
---

<objective>
Fix three UAT gaps in the status executor: (1) summary line missing behind/ahead repo counts, (2) 'clean' label replaced with more descriptive 'ok', (3) tag-pinned repos missing their own warning type.

Purpose: UAT Tests 1 and 4 reported issues with the status output. The summary line only shows configured/synced/not-synced but discards the AheadBehind data before summarizing. Tag-pinned repos are excluded from the detached HEAD warning by the `isDetachedHead && !isTagPinned` guard (line 227) and have no alternative warning.

Output: Patched status executor and updated tests passing all scenarios.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-repo-git-dx/03-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Already imported in the file. -->

From packages/op-nx-polyrepo/src/lib/git/detect.ts:

```typescript
export interface AheadBehind {
  ahead: number;
  behind: number;
}
export interface WorkingTreeState {
  modified: number;
  staged: number;
  deleted: number;
  untracked: number;
  conflicts: number;
}
export async function getCurrentBranch(cwd: string): Promise<string | null>;
export async function getAheadBehind(cwd: string): Promise<AheadBehind | null>;
```

From packages/op-nx-polyrepo/src/lib/executors/status/executor.ts (current structure):

```typescript
interface RepoRowData {
  alias: string;
  branchDisplay: string;
  aheadBehind: string;
  dirtySummary: string;
  projectCount: string;
  warnings: string;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix status executor summary line, dirty label, and tag-pinned warning</name>
  <files>packages/op-nx-polyrepo/src/lib/executors/status/executor.ts, packages/op-nx-polyrepo/src/lib/executors/status/executor.spec.ts</files>
  <behavior>
    - Test: summary line includes "N behind" when N repos have behind>0 (e.g. "1 behind")
    - Test: summary line includes "N ahead" when N repos have ahead>0 (e.g. "2 ahead")
    - Test: summary line omits behind/ahead phrases when all repos are 0/0
    - Test: clean repos display "ok" instead of "clean" in dirty summary column
    - Test: tag-pinned repo shows "[WARN: tag-pinned]" in warnings column
    - Test: tag-pinned + dirty repo shows both "[WARN: dirty, sync may fail] [WARN: tag-pinned]"
  </behavior>
  <action>
**Changes to executor.ts:**

1. **Replace 'clean' with 'ok'** in `formatDirtySummary` (line 90): Change the fallback return from `'clean'` to `'ok'`.

2. **Add raw AheadBehind to RepoRowData**: Add a new field `rawAheadBehind: AheadBehind | null` to the `RepoRowData` interface. Store the raw `aheadBehind` object alongside the formatted display string in the synced repo processing loop (around line 239).

3. **Add tag-pinned warning**: At line 227, after the existing `if (isDetachedHead && !isTagPinned)` block, add a new block:

   ```typescript
   if (isTagPinned) {
     warnings.push('[WARN: tag-pinned]');
   }
   ```

   This gives tag-pinned repos their own distinct warning instead of being silently excluded.

4. **Enhance summary line** (lines 285-293): After the existing summary line, aggregate the raw AheadBehind data from `rowData`:
   - Count repos where `rawAheadBehind !== null && rawAheadBehind.behind > 0` into `reposBehind`
   - Count repos where `rawAheadBehind !== null && rawAheadBehind.ahead > 0` into `reposAhead`
   - Build an array of extra summary parts: push `"N behind"` if reposBehind > 0, push `"N ahead"` if reposAhead > 0
   - Append these parts to the existing summary string with `, ` separator. Example output: `"3 configured, 2 synced, 1 not synced, 1 behind"` or `"3 configured, 2 synced, 0 not synced, 1 behind, 2 ahead"`

**Changes to executor.spec.ts:**

1. Update the existing `'shows clean when all working tree counts are zero'` test to assert `'ok'` instead of `'clean'`.

2. Update the existing `'shows branch, ahead/behind, clean status, and project count for synced remote repo'` test to assert `'ok'` instead of `'clean'`.

3. Add test: `'summary line includes behind count when repos are behind remote'` -- set up 2 repos, one with behind=3, verify summary line contains "1 behind".

4. Add test: `'summary line includes ahead count when repos are ahead of remote'` -- set up 1 repo with ahead=2, verify summary line contains "1 ahead".

5. Add test: `'summary line omits behind/ahead when all repos are even'` -- verify summary line does NOT contain "behind" or "ahead".

6. Add test: `'shows [WARN: tag-pinned] for tag-pinned repo'` -- set up tag-pinned repo (branch=null, ref=v2.1.0), verify warnings column contains "[WARN: tag-pinned]".

7. Add test: `'shows both dirty and tag-pinned warnings when tag-pinned repo is dirty'` -- set up tag-pinned repo with dirty tree, verify warnings contain both "[WARN: dirty, sync may fail]" and "[WARN: tag-pinned]".
   </action>
   <verify>
   <automated>npm exec nx test @op-nx/polyrepo -- --reporter=verbose 2>&1 | tail -40</automated>
   </verify>
   <done> - All existing tests pass (with 'clean' assertions updated to 'ok') - 5 new tests pass for behind/ahead summary, tag-pinned warning, tag-pinned+dirty combo - Summary line shows "N behind" / "N ahead" when applicable - Tag-pinned repos get [WARN: tag-pinned] instead of no warning
   </done>
   </task>

</tasks>

<verification>
- `npm exec nx test @op-nx/polyrepo` passes all tests
- Summary line includes behind/ahead counts when present
- Tag-pinned repos show [WARN: tag-pinned]
- Clean repos show "ok" not "clean"
</verification>

<success_criteria>

- UAT Test 1 gap closed: summary line now includes repo-level behind/ahead counts
- UAT Test 4 gap closed: tag-pinned repos show [WARN: tag-pinned] warning
- All status executor tests pass including new assertions
  </success_criteria>

<output>
After completion, create `.planning/phases/03-multi-repo-git-dx/03-04-SUMMARY.md`
</output>
