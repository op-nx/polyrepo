---
phase: 05-avoid-type-casting-and-prefer-satisfies
plan: 04
type: execute
wave: 2
depends_on: ['05-01']
files_modified:
  - packages/op-nx-polyrepo/src/lib/git/commands.spec.ts
  - packages/op-nx-polyrepo/src/lib/git/detect.spec.ts
  - packages/op-nx-polyrepo/src/lib/git/normalize-url.spec.ts
  - packages/op-nx-polyrepo/src/lib/graph/extract.spec.ts
  - packages/op-nx-polyrepo/src/lib/graph/cache.spec.ts
  - packages/op-nx-polyrepo/src/lib/graph/transform.spec.ts
  - packages/op-nx-polyrepo/src/lib/config/resolve.spec.ts
  - packages/op-nx-polyrepo/src/lib/config/schema.spec.ts
  - packages/op-nx-polyrepo/src/lib/config/validate.spec.ts
  - packages/op-nx-polyrepo/src/lib/format/table.spec.ts
  - packages/op-nx-polyrepo/src/index.spec.ts
autonomous: true
requirements:
  - SAFE-CASTS
  - SAFE-SIFER

must_haves:
  truths:
    - 'Zero eslint-disable comments in git/, graph/, config/, format/, and index test files'
    - 'All overloaded function mock casts in git/ tests are eliminated'
    - 'All stub object casts ({} as never) are replaced with typed factories'
    - 'All beforeEach hooks in these files are replaced with SIFER setup functions'
    - 'All tests still pass after refactoring'
  artifacts:
    - path: 'packages/op-nx-polyrepo/src/lib/git/detect.spec.ts'
      provides: 'Cast-free SIFER-pattern tests for git detection'
    - path: 'packages/op-nx-polyrepo/src/lib/git/commands.spec.ts'
      provides: 'Cast-free SIFER-pattern tests for git commands'
    - path: 'packages/op-nx-polyrepo/src/lib/graph/extract.spec.ts'
      provides: 'Cast-free SIFER-pattern tests for graph extraction'
  key_links:
    - from: 'packages/op-nx-polyrepo/src/lib/git/detect.spec.ts'
      to: 'vi.mocked()'
      via: 'Vitest 4.x overloaded mock fix -- no as casts'
      pattern: "vi\\.mocked"
---

<objective>
Refactor 11 non-executor test files to eliminate ALL type casting eslint-disable comments and adopt the SIFER pattern. This covers: git/ (commands, detect, normalize-url), graph/ (extract, cache, transform), config/ (resolve, schema, validate), format/ (table), and index.spec.ts.

Purpose: These files contain 13 of 18 total eslint-disable comments (9 in detect.spec.ts, 2 in commands.spec.ts, 2 in extract.spec.ts). Eliminating casts requires Vitest 4.x mock APIs and typed factory functions.
Output: 11 test files with zero eslint-disable comments, zero beforeEach hooks, all tests passing.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-RESEARCH.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-01-SUMMARY.md

@packages/op-nx-polyrepo/src/lib/git/detect.spec.ts
@packages/op-nx-polyrepo/src/lib/git/commands.spec.ts
@packages/op-nx-polyrepo/src/lib/graph/extract.spec.ts
@packages/op-nx-polyrepo/src/index.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminate overloaded mock casts in git/ test files and refactor to SIFERs</name>
  <files>
    packages/op-nx-polyrepo/src/lib/git/commands.spec.ts,
    packages/op-nx-polyrepo/src/lib/git/detect.spec.ts,
    packages/op-nx-polyrepo/src/lib/git/normalize-url.spec.ts
  </files>
  <action>
**Step 1: Test the Vitest 4.x overloaded mock fix (Open Question 1 from RESEARCH.md)**

In commands.spec.ts or detect.spec.ts, find one `eslint-disable` comment for an overloaded function mock cast (e.g., `vi.fn().mockImplementation(callback as typeof execFile)`). Remove the cast and the eslint-disable comment. Run typecheck:

```
npm exec nx typecheck @op-nx/polyrepo
```

- **If it compiles:** Strategy A works. The `vi.mocked()` / `vi.fn()` in Vitest 4.x correctly resolves the overloaded function type. Apply this pattern to ALL overloaded mock casts.
- **If it fails:** Strategy B needed. Create typed mock helper functions per the RESEARCH.md "Wrapper function approach" pattern. For each overloaded function (`execFile`, `exec`), create a helper like:
  ```typescript
  function mockExecCallback(
    impl: (
      cmd: string,
      options: ExecOptions,
      callback: (
        error: ExecException | null,
        stdout: string,
        stderr: string,
      ) => void,
    ) => void,
  ): void {
    vi.mocked(exec).mockImplementation(impl);
  }
  ```

**Step 2: Refactor commands.spec.ts (548 lines, 2 eslint-disables)**

- Remove ALL `eslint-disable` comments (2 for overloaded function mock casts on lines 35 and 280)
- Replace ALL `beforeEach` hooks with SIFER `setup()` functions per the RESEARCH.md SIFER pattern:
  - Each `describe` block's `beforeEach` becomes a `setup()` function that returns all test state
  - `vi.clearAllMocks()` moves inside setup
  - Each `it()` call starts with `const { mockExec, ... } = setup()`
  - Setup functions accept optional override parameters for per-test customization
- Fix any other new lint violations (consistent-type-imports, explicit return types not needed in tests, etc.)

**Step 3: Refactor detect.spec.ts (695 lines, 9 eslint-disables)**

- Remove ALL 9 `eslint-disable` comments (all for overloaded function mock casts)
- Apply the same overloaded mock strategy determined in Step 1
- Replace ALL `beforeEach` hooks with SIFER setup functions
- Fix any other new lint violations

**Step 4: Refactor normalize-url.spec.ts (54 lines, 0 eslint-disables)**

- This file has no eslint-disable comments but may have `beforeEach` hooks
- Convert to SIFER pattern if hooks exist
- Fix any new lint violations

After each file, run tests to verify: `npm exec nx test @op-nx/polyrepo`
</action>
<verify>
<automated>npm exec nx test @op-nx/polyrepo &amp;&amp; git grep -c "eslint-disable" -- packages/op-nx-polyrepo/src/lib/git/\*.spec.ts 2>&amp;1 || echo "[OK] no eslint-disable in git tests"</automated>
</verify>
<done>Zero eslint-disable comments in git/ test files. Zero beforeEach hooks. All overloaded mock casts eliminated via Vitest 4.x APIs or typed helpers. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor graph/, config/, format/, and index test files to SIFERs and fix all violations</name>
  <files>
    packages/op-nx-polyrepo/src/lib/graph/extract.spec.ts,
    packages/op-nx-polyrepo/src/lib/graph/cache.spec.ts,
    packages/op-nx-polyrepo/src/lib/graph/transform.spec.ts,
    packages/op-nx-polyrepo/src/lib/config/resolve.spec.ts,
    packages/op-nx-polyrepo/src/lib/config/schema.spec.ts,
    packages/op-nx-polyrepo/src/lib/config/validate.spec.ts,
    packages/op-nx-polyrepo/src/lib/format/table.spec.ts,
    packages/op-nx-polyrepo/src/index.spec.ts
  </files>
  <action>
**Refactor each file following the same pattern:**

1. Remove ALL eslint-disable comments:
   - extract.spec.ts: 2 comments (overloaded mock casts -- use same strategy from Task 1)
   - index.spec.ts: 1 comment (`{} as never` stub -- replace with typed factory)

2. Replace `{} as never` stub objects with typed factories:
   - In index.spec.ts, the stub is for graph nodes not inspected by SUT. Create a factory:
     ```typescript
     function createStubNode(name: string): ProjectGraphProjectNode {
       return { name, type: 'lib', data: { root: `libs/${name}` } };
     }
     ```
   - Use the actual type from @nx/devkit (ProjectGraphProjectNode or the transformed type) -- do NOT use `never` or `any`.

3. Replace ALL `beforeEach` hooks with SIFER setup functions in every file:
   - extract.spec.ts (205 lines): Has beforeEach with mock setup for exec
   - cache.spec.ts (440 lines): Has beforeEach for filesystem mocks
   - transform.spec.ts (441 lines): May have beforeEach for graph data setup
   - resolve.spec.ts (72 lines): Has beforeEach for readFileSync mock
   - schema.spec.ts (421 lines): May have beforeEach
   - validate.spec.ts (146 lines): Has beforeEach for Zod validation mocks
   - table.spec.ts (99 lines): May have beforeEach
   - index.spec.ts: Has beforeEach for module mocks

4. Fix all remaining lint violations:
   - `consistent-type-imports` -- autofix with `--fix` first
   - Any `no-unsafe-*` violations from mock patterns
   - Any new violations from strict-type-checked rules

**Workflow per file:**

1. Read the file
2. Remove eslint-disable comments
3. Replace casts with proper typing (factory or vi.mocked)
4. Convert beforeEach to SIFER setup()
5. Run `npm exec nx test @op-nx/polyrepo` to verify
6. Run `npm exec nx lint @op-nx/polyrepo -- --fix` for autofixes
   </action>
   <verify>
   <automated>npm exec nx test @op-nx/polyrepo &amp;&amp; git grep -c "eslint-disable" -- packages/op-nx-polyrepo/src/lib/graph/_.spec.ts packages/op-nx-polyrepo/src/lib/config/_.spec.ts packages/op-nx-polyrepo/src/lib/format/\*.spec.ts packages/op-nx-polyrepo/src/index.spec.ts 2>&amp;1 || echo "[OK] no eslint-disable in these test files"</automated>
   </verify>
   <done>Zero eslint-disable comments in graph/, config/, format/, and index test files. Zero beforeEach hooks. All stub casts replaced with typed factories. All tests pass.</done>
   </task>

</tasks>

<verification>
- `npm exec nx test @op-nx/polyrepo` passes (all tests green)
- `git grep "eslint-disable" -- packages/op-nx-polyrepo/src/lib/git/ packages/op-nx-polyrepo/src/lib/graph/ packages/op-nx-polyrepo/src/lib/config/ packages/op-nx-polyrepo/src/lib/format/ packages/op-nx-polyrepo/src/index.spec.ts` returns empty
- `git grep "beforeEach\|afterEach" -- packages/op-nx-polyrepo/src/lib/git/*.spec.ts packages/op-nx-polyrepo/src/lib/graph/*.spec.ts packages/op-nx-polyrepo/src/lib/config/*.spec.ts packages/op-nx-polyrepo/src/lib/format/*.spec.ts packages/op-nx-polyrepo/src/index.spec.ts` returns empty
</verification>

<success_criteria>

- Zero eslint-disable comments across all 11 test files in this plan
- Zero beforeEach/afterEach hooks in these files
- All tests pass
- Overloaded mock casting strategy determined and documented (Strategy A vs B)
- Stub objects use typed factories instead of `{} as never`
  </success_criteria>

<output>
After completion, create `.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-04-SUMMARY.md`
</output>
