---
phase: 10-integration-and-end-to-end-validation
task: e2e-perf-optimization
total_tasks: 5
status: in_progress
last_updated: "2026-03-19T20:53:57.866Z"
---

<current_state>
Milestone v1.1 feature work is COMPLETE (all 7 e2e + 333 unit tests pass).
Currently optimizing e2e setup performance. Docker base image was just rebuilt
from cold (pruned all Docker state). Cleanup logic committed. Plan written but
no dissection work started yet.
</current_state>

<completed_work>

## fileMap guard fix (DONE)
- namedInputs override on external projects (all workspace-level named inputs → [])
- Cross-repo edges target project nodes directly (externalNodes approach abandoned — nx graph --print prunes them)
- Cache key uses only repos hash (not full options hash)
- All 7 e2e tests + 333 unit tests pass

## e2e infra improvements (DONE)
- Parallelized global setup phases 1-3 (image/network, Verdaccio/workspace, publish/nx.json)
- NX_DAEMON=false for host-side publish (prevents daemon graph lock)
- Per-phase timing instrumentation (timed() helper)
- Crash-resilient Docker cleanup in setup + teardown
- Pre-version command uses --exclude-task-dependencies (prevents ^build cascading)
- Kebab-case for all Nx CLI flags
- baseUrl added to tsconfig.lib.json (fixes TS5090 with @nx/js:tsc)
- Cached graph JSON fast path in extractGraphFromRepo (reads .nx-graph-output.json)

## Research (DONE)
- filemap-guard-nx-source.md — Rust task hasher trace
- external-nodes-api.md — externalNodes API analysis
- filemap-guard-SUMMARY.md — decision matrix
- e2e-graph-cache-perf.md — graph cache warm analysis
- e2e-snapshot-perf.md — Docker commit optimization
- module-resolution-e2e.md — npm/pnpm/Yarn PnP comparison
</completed_work>

<remaining_work>

## Phase A: Dissect snapshot commit (3m33s)
- Start container, simulate setup steps manually
- Run `docker diff` after each step to see writable layer contents
- Time `docker commit` at each stage to isolate what dominates
- Check if ~/.npm/_cacache/ is a bigger contributor than node_modules
- Determine if node_modules deletion is actually helping or if npm cache is the real issue

## Phase B: Dissect graph cache warm (4m43s)
- Add NX_PERF_LOGGING=true to nx show projects exec in container
- Capture stderr for per-plugin timing
- Confirm whether 4m43s is child nx graph --print OR host Nx processing
- Previous diagnostic showed extractGraphFromRepo is NOT called (disk cache hit)
  so the 4m43s is host-side Nx processing 149 nodes through all plugins

## Phase C: Gradle optimization
- Verify ~/.gradle/ and .gradle/ survive Docker COPY to .repos/nx/
- Set GRADLE_OPTS=-Dorg.gradle.daemon=false in container ENV
- Measure impact of warm Gradle disk cache + no-daemon mode
- If still slow: run nx graph --print in Dockerfile FINAL stage (correct path)
- Research best JVM for Gradle in our Docker setup: Linux arm64 containers
  on Windows arm64 via WSL2. Compare OpenJDK, Eclipse Temurin, GraalVM, etc.
  for cold start time and memory footprint. The Dockerfile currently installs
  default-jre-headless (Debian's default, likely OpenJDK). A faster-starting
  JVM could shave 10-30s off Gradle cold start.

## Phase D: Apply remaining research recommendations
- Research containerd snapshotter v1 diff behavior (not legacy overlay2)
- Pre-bake node_modules into image layer (dummy plugin install in Dockerfile)
- Re-evaluate node_modules deletion based on Phase A findings

## Phase E: Integrate & measure
- Stack winning improvements
- Run full e2e, compare per-phase timing against baseline
- Commit with results

## Decision: revert or keep node_modules deletion
- Current result: only ~30s improvement on commit, +8s on tests (net wash)
- Need Phase A data to decide — might be that ~/.npm/_cacache/ is the real bottleneck
</remaining_work>

<decisions_made>

- namedInputs must override ALL workspace-level named inputs (not just "default") because ^production expands negation file patterns that trigger ProjectFileSet
- Cross-repo edges must target project nodes (not externalNodes) because nx graph --print prunes externalNodes from output
- extractGraphFromRepo is NOT the bottleneck — our disk cache works, the 4m43s is host-side Nx plugin processing
- node_modules deletion before snapshot only saved ~30s (not the expected 2-3min) — need docker diff data to understand why
- Gradle: cannot disable @nx/gradle — plugin must support polyglot workspaces
- Docker cleanup runs at START of setup (crash resilience) + END of teardown
</decisions_made>

<blockers>

- Docker Desktop repeatedly drops connection during long e2e runs (socket hang up / ECONNRESET). May be related to WSL2 memory pressure from accumulated Docker state (was at 10GB before prune reclaimed 76GB).
- containerd snapshotter v1 diff behavior not researched — NaiveDiffDriver research may not apply
</blockers>

<context>
The e2e setup baseline is ~8m13s with two ~equal bottlenecks:
- Graph cache warm: 4m43s (Nx processing 149 nodes, NOT extraction)
- Snapshot commit: 3m33s (overlay2 diff on writable layer)

The cached JSON approach (reading .nx-graph-output.json) works but is irrelevant — extraction
is already cached by our disk cache. The bottleneck is Nx's own plugin pipeline processing
149 project registrations.

The node_modules deletion approach showed minimal improvement (~30s). Need docker diff
dissection to understand what's actually in the writable layer.

Docker base image was just rebuilt from cold (all Docker state pruned). The image tag
is op-nx-e2e-workspace:latest.

Three research files exist in .planning/research/ covering all findings.
</context>

<next_action>
Start with Phase A: dissect the snapshot commit.
1. docker run -d --name e2e-dissect op-nx-e2e-workspace sleep infinity
2. docker diff e2e-dissect (baseline — should be empty or minimal)
3. Simulate npm install, check docker diff, time docker commit
4. Simulate nx show projects, check docker diff, time docker commit
5. Test rm -rf node_modules, check docker diff, time docker commit
6. Record results in a table, decide on optimization strategy
</next_action>
