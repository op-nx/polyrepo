---
phase: 05-avoid-type-casting-and-prefer-satisfies
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/op-nx-polyrepo/src/lib/executors/status/executor.spec.ts
  - packages/op-nx-polyrepo/src/lib/executors/sync/executor.spec.ts
  - packages/op-nx-polyrepo/src/lib/executors/run/executor.spec.ts
autonomous: true
requirements:
  - SAFE-CASTS
  - SAFE-SIFER

must_haves:
  truths:
    - "Zero eslint-disable comments in executor test files"
    - "Partial ExecutorContext casts replaced with factory function"
    - "EventEmitter-as-ChildProcess casts replaced with typed mock factory"
    - "All stub node casts replaced with typed factories"
    - "All beforeEach hooks replaced with SIFER setup functions"
    - "All executor tests still pass"
  artifacts:
    - path: "packages/op-nx-polyrepo/src/lib/executors/sync/executor.spec.ts"
      provides: "Cast-free SIFER-pattern tests for sync executor"
    - path: "packages/op-nx-polyrepo/src/lib/executors/status/executor.spec.ts"
      provides: "Cast-free SIFER-pattern tests for status executor"
    - path: "packages/op-nx-polyrepo/src/lib/executors/run/executor.spec.ts"
      provides: "Cast-free SIFER-pattern tests for run executor"
  key_links:
    - from: "packages/op-nx-polyrepo/src/lib/executors/sync/executor.spec.ts"
      to: "createTestContext factory"
      via: "Replaces partial ExecutorContext cast"
      pattern: "createTestContext"
    - from: "packages/op-nx-polyrepo/src/lib/executors/sync/executor.spec.ts"
      to: "createMockChildProcess factory"
      via: "Replaces EventEmitter as ChildProcess cast"
      pattern: "createMockChildProcess"
---

<objective>
Refactor the 3 executor test files to eliminate ALL type casting eslint-disable comments and adopt the SIFER pattern. These are the largest and most complex test files: sync/executor.spec.ts (1783 lines, 2 eslint-disables), status/executor.spec.ts (837 lines, 2 eslint-disables), run/executor.spec.ts (223 lines).

Purpose: Executor tests have the hardest cast patterns: partial ExecutorContext objects, EventEmitter-as-ChildProcess mocks, and stub graph nodes. These require carefully typed factory functions.
Output: 3 executor test files with zero eslint-disable comments, zero beforeEach hooks, all tests passing.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-RESEARCH.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-01-SUMMARY.md

@packages/op-nx-polyrepo/src/lib/executors/sync/executor.spec.ts
@packages/op-nx-polyrepo/src/lib/executors/status/executor.spec.ts
@packages/op-nx-polyrepo/src/lib/executors/run/executor.spec.ts
@packages/op-nx-polyrepo/src/lib/executors/sync/executor.ts
@packages/op-nx-polyrepo/src/lib/executors/status/executor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor status and run executor tests -- factories, cast elimination, SIFERs</name>
  <files>
    packages/op-nx-polyrepo/src/lib/executors/status/executor.spec.ts,
    packages/op-nx-polyrepo/src/lib/executors/run/executor.spec.ts
  </files>
  <action>
**status/executor.spec.ts (837 lines, 2 eslint-disables):**

1. **Partial ExecutorContext cast** (line 77: `{ root, cwd } as ExecutorContext`):
   Create a `createTestContext` factory function returning a full `ExecutorContext`:
   ```typescript
   function createTestContext(overrides?: Partial<ExecutorContext>): ExecutorContext {
     return {
       root: '/workspace',
       cwd: '/workspace',
       isVerbose: false,
       projectsConfigurations: { version: 2, projects: {} },
       nxJsonConfiguration: {},
       projectGraph: { nodes: {}, dependencies: {} },
       ...overrides,
     };
   }
   ```
   NOTE: Check the actual `ExecutorContext` interface from `@nx/devkit` for all required fields. The factory must provide ALL required fields with sensible defaults. If `exactOptionalPropertyTypes` is enabled, ensure no `undefined` is assigned to optional-only properties.

2. **Stub node casts** (line 158: `{} as never` for graph nodes):
   Create a `createStubNode` factory. Check what type the status executor actually expects for graph nodes and provide all required fields.

3. **SIFER refactoring**: Replace the `beforeEach` (around line 134) with a `setup()` function. The setup should:
   - Call `vi.clearAllMocks()`
   - Configure all mocks (resolvePluginConfig, getWorkingTreeState, etc.)
   - Return all mocked functions and test context
   - Accept override parameters for per-test customization

4. Fix all other lint violations (consistent-type-imports, etc.).

**run/executor.spec.ts (223 lines, 0 eslint-disables):**

1. No eslint-disable comments but has `beforeEach` hooks.
2. Convert to SIFER pattern.
3. Fix any new lint violations from strict rules.

Run tests after each file: `npm exec nx test @op-nx/polyrepo`
  </action>
  <verify>
    <automated>npm exec nx test @op-nx/polyrepo &amp;&amp; git grep -c "eslint-disable" -- packages/op-nx-polyrepo/src/lib/executors/status/executor.spec.ts packages/op-nx-polyrepo/src/lib/executors/run/executor.spec.ts 2>&amp;1 || echo "[OK] no eslint-disable"</automated>
  </verify>
  <done>Zero eslint-disable comments in status and run executor tests. Zero beforeEach hooks. Partial ExecutorContext and stub node casts replaced with typed factories. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor sync executor tests -- ChildProcess mock factory, cast elimination, SIFERs</name>
  <files>
    packages/op-nx-polyrepo/src/lib/executors/sync/executor.spec.ts
  </files>
  <action>
**sync/executor.spec.ts (1783 lines, 2 eslint-disables):**

This is the largest and most complex test file. It has two cast patterns:

1. **EventEmitter-as-ChildProcess cast** (line 86 area: block-scoped eslint-disable for ChildProcess mocking):
   The current code creates an `EventEmitter` and casts it to `ChildProcess`. Replace with a `createMockChildProcess` factory that returns a properly typed object. Per RESEARCH.md:
   - Create a factory that constructs an object with all required ChildProcess properties
   - Use `Object.create(EventEmitter.prototype)` as the base
   - Add `Object.defineProperty` for required properties (stdin, stdout, stderr, pid, exitCode, etc.)
   - The factory returns `ChildProcess` type without casting
   - If ChildProcess has too many required properties (20+), an alternative is to use the actual `EventEmitter` approach but type-assert once inside the factory (the factory return type is the contract, internal implementation is encapsulated). However, try the full-object approach first.

2. **Partial ExecutorContext cast** (line 130: `{ root, cwd, isVerbose } as ExecutorContext`):
   Reuse the same `createTestContext` factory pattern from Task 1. If this file and status.spec.ts need the same factory, consider placing it at a shared location (e.g., a test-helpers file). However, per the user's preference for simple factory functions, duplicating a small factory across files is acceptable.

3. **SIFER refactoring**: Replace the `beforeEach` (around line 154) with a `setup()` function. This is the most complex setup -- it configures spawn mocks, exec mocks, filesystem mocks, and config mocks. Structure the setup to:
   - Accept an options object for customizing mock behavior per test
   - Return all mocked functions the tests reference
   - Move `vi.clearAllMocks()` inside setup
   - Handle the mock ChildProcess creation inside setup

4. Fix all other lint violations.

NOTE: This is a large file. Work methodically:
1. First, create the factories (createMockChildProcess, createTestContext)
2. Then replace all cast sites with factory calls
3. Then convert beforeEach to SIFER setup()
4. Then fix remaining lint violations
5. Run tests after each major change

Run tests: `npm exec nx test @op-nx/polyrepo`
  </action>
  <verify>
    <automated>npm exec nx test @op-nx/polyrepo &amp;&amp; git grep -c "eslint-disable" -- packages/op-nx-polyrepo/src/lib/executors/sync/executor.spec.ts 2>&amp;1 || echo "[OK] no eslint-disable"</automated>
  </verify>
  <done>Zero eslint-disable comments in sync executor test. Zero beforeEach hooks. EventEmitter-ChildProcess cast eliminated via typed factory. Partial ExecutorContext cast eliminated via factory. All 1783+ lines of tests pass.</done>
</task>

</tasks>

<verification>
- `npm exec nx test @op-nx/polyrepo` passes (all tests green)
- `git grep "eslint-disable" -- packages/op-nx-polyrepo/src/lib/executors/` returns empty
- `git grep "beforeEach\|afterEach" -- packages/op-nx-polyrepo/src/lib/executors/*.spec.ts packages/op-nx-polyrepo/src/lib/executors/**/*.spec.ts` returns empty
</verification>

<success_criteria>
- Zero eslint-disable comments across all 3 executor test files
- Zero beforeEach/afterEach hooks
- Partial ExecutorContext replaced with createTestContext factory
- EventEmitter-ChildProcess mock replaced with createMockChildProcess factory
- Stub graph nodes replaced with typed factories
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-05-SUMMARY.md`
</output>
