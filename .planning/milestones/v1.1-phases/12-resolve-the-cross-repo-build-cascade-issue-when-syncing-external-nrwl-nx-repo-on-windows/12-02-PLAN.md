---
phase: 12-resolve-the-cross-repo-build-cascade-issue-when-syncing-external-nrwl-nx-repo-on-windows
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - nx.json
autonomous: false
requirements:
  - BUILD-02

must_haves:
  truths:
    - 'nx test @op-nx/polyrepo succeeds without --exclude-task-dependencies'
    - 'nx/devkit:build succeeds via proxy executor on Windows'
    - 'preVersionCommand in nx.json is evaluated for --exclude-task-dependencies removal'
  artifacts:
    - path: 'nx.json'
      provides: 'Clean preVersionCommand without unnecessary --exclude-task-dependencies (if testing proves safe)'
      contains: 'preVersionCommand'
  key_links:
    - from: 'nx.json targetDefaults'
      to: 'proxy targets in graph'
      via: 'Nx merge logic respects explicit dependsOn on proxy targets'
      pattern: 'targetDefaults'
---

<objective>
Verify the full fix works end-to-end and clean up the --exclude-task-dependencies workaround.

Purpose: Confirm that `nx test @op-nx/polyrepo` succeeds without the `--exclude-task-dependencies` workaround now that targetDefaults isolation and env isolation are in place. Remove the workaround from `nx.json` preVersionCommand if testing proves it redundant.

Output: Verified working `nx test` without workarounds, cleaned-up nx.json.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-resolve-the-cross-repo-build-cascade-issue-when-syncing-external-nrwl-nx-repo-on-windows/12-CONTEXT.md
@.planning/phases/12-resolve-the-cross-repo-build-cascade-issue-when-syncing-external-nrwl-nx-repo-on-windows/12-RESEARCH.md
@.planning/phases/12-resolve-the-cross-repo-build-cascade-issue-when-syncing-external-nrwl-nx-repo-on-windows/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify nx test works without --exclude-task-dependencies and evaluate preVersionCommand</name>
  <files>nx.json</files>
  <action>
    **Step 1: Build the plugin first (required before testing)**

    Run `npm exec nx -- build @op-nx/polyrepo` to ensure the latest code (with dependsOn preservation and env isolation) is compiled.

    **Step 2: Verify nx test without workaround**

    Run `npm exec nx -- test @op-nx/polyrepo` (without `--exclude-task-dependencies`). This should now succeed because:
    - Host `targetDefaults.test.dependsOn: ["^build"]` no longer leaks into external proxy targets (they have explicit `dependsOn`)
    - The only legitimate cross-repo cascade is `@op-nx/polyrepo -> @nx/devkit -> nx/devkit:build`
    - `nx/devkit:build` succeeds via proxy executor because child Nx gets `NX_DAEMON=false` and isolated `NX_WORKSPACE_DATA_DIRECTORY`

    If the test fails due to a timeout, try adding `NX_PLUGIN_NO_TIMEOUTS=true` as env prefix.

    **Step 3: Evaluate preVersionCommand**

    Read `nx.json` line 74: `"preVersionCommand": "npx nx run-many -t build --exclude tag:polyrepo:external --exclude-task-dependencies"`

    Analyze whether `--exclude-task-dependencies` is still needed:
    - The `--exclude tag:polyrepo:external` already filters ALL external projects from the `run-many` target list.
    - After the fix, host projects that depend on external projects (e.g., `@op-nx/polyrepo` -> `@nx/devkit`) will cascade into the proxy executor, which now works correctly.
    - `--exclude-task-dependencies` prevented ANY task dependency from running (including host `^build` between host packages). This was overly aggressive.
    - If `--exclude tag:polyrepo:external` alone is sufficient (the cascade through proxy executor now works), remove `--exclude-task-dependencies`.

    **Step 4: Test preVersionCommand**

    Run the build command manually to verify:
    ```bash
    npm exec nx -- run-many -t build --exclude tag:polyrepo:external
    ```

    If this succeeds, update `nx.json` to remove `--exclude-task-dependencies` from the preVersionCommand:
    ```json
    "preVersionCommand": "npx nx run-many -t build --exclude tag:polyrepo:external"
    ```

    If it fails (e.g., cascade through `@nx/devkit:build` times out during `run-many`), KEEP `--exclude-task-dependencies` and document why in the summary.

    **Step 5: Final verification**

    Run one more time to confirm the full suite is green:
    ```bash
    npm exec nx -- test @op-nx/polyrepo
    npm exec nx -- lint @op-nx/polyrepo
    ```

  </action>
  <verify>
    <automated>npm exec nx -- test @op-nx/polyrepo</automated>
  </verify>
  <done>
    - `npm exec nx -- test @op-nx/polyrepo` passes without `--exclude-task-dependencies`
    - preVersionCommand evaluated and updated if safe (or documented why kept)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of end-to-end fix</name>
  <files>nx.json</files>
  <action>
    Verify the full targetDefaults isolation and Windows build fix:
    1. Proxy targets now preserve dependsOn from external repo config (with project name namespacing)
    2. Proxy targets without dependsOn get explicit empty array (blocks host targetDefaults)
    3. Proxy executor passes NX_DAEMON=false and NX_WORKSPACE_DATA_DIRECTORY to child processes
    4. preVersionCommand evaluated for --exclude-task-dependencies removal

    Steps for user to verify:
    1. Run `npm exec nx -- test @op-nx/polyrepo` -- should succeed without any workarounds
    2. Run `npm exec nx -- build @op-nx/polyrepo` -- should succeed (includes ^build cascade to nx/devkit:build via proxy)
    3. Verify in the project graph that external targets no longer inherit host targetDefaults:
       - Run `npm exec nx -- show project nx/devkit --json` and check that the `test` target does NOT have `dependsOn: ["^build"]` from the host (it should have its own dependsOn from the nrwl/nx repo, or `[]`)
    4. If preVersionCommand was changed, run `npm exec nx -- run-many -t build --exclude tag:polyrepo:external` to verify it still works

  </action>
  <verify>User confirms the fix works correctly</verify>
  <done>User approves the end-to-end fix</done>
</task>

</tasks>

<verification>
```bash
# Full test suite
npm exec nx -- test @op-nx/polyrepo

# Lint

npm exec nx -- lint @op-nx/polyrepo

# Build (triggers cross-repo cascade through proxy)

npm exec nx -- build @op-nx/polyrepo

```
</verification>

<success_criteria>
- `nx test @op-nx/polyrepo` passes without `--exclude-task-dependencies`
- `nx build @op-nx/polyrepo` succeeds with cross-repo cascade to nx/devkit:build
- preVersionCommand evaluated and either cleaned up or documented
- User verifies the end-to-end fix
</success_criteria>

<output>
After completion, create `.planning/phases/12-resolve-the-cross-repo-build-cascade-issue-when-syncing-external-nrwl-nx-repo-on-windows/12-02-SUMMARY.md`
</output>
```
