---
phase: 05-avoid-type-casting-and-prefer-satisfies
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/op-nx-polyrepo/src/index.ts
  - packages/op-nx-polyrepo/src/lib/config/resolve.ts
  - packages/op-nx-polyrepo/src/lib/config/schema.ts
  - packages/op-nx-polyrepo/src/lib/config/validate.ts
  - packages/op-nx-polyrepo/src/lib/executors/run/executor.ts
  - packages/op-nx-polyrepo/src/lib/executors/status/executor.ts
  - packages/op-nx-polyrepo/src/lib/executors/sync/executor.ts
  - packages/op-nx-polyrepo/src/lib/format/table.ts
  - packages/op-nx-polyrepo/src/lib/git/commands.ts
  - packages/op-nx-polyrepo/src/lib/git/detect.ts
  - packages/op-nx-polyrepo/src/lib/git/normalize-url.ts
  - packages/op-nx-polyrepo/src/lib/git/patterns.ts
  - packages/op-nx-polyrepo/src/lib/graph/cache.ts
  - packages/op-nx-polyrepo/src/lib/graph/extract.ts
  - packages/op-nx-polyrepo/src/lib/graph/transform.ts
  - packages/op-nx-polyrepo/src/lib/graph/types.ts
autonomous: true
requirements:
  - SAFE-ANY
  - SAFE-TYPES

must_haves:
  truths:
    - "All production .ts files pass lint and typecheck with strict rules"
    - "No explicit any types in production code"
    - "All production functions have explicit return type annotations"
    - "Type-only imports use import type syntax"
    - "Type-only exports use export type syntax"
  artifacts:
    - path: "packages/op-nx-polyrepo/src/index.ts"
      provides: "Clean exports with type qualifiers"
    - path: "packages/op-nx-polyrepo/src/lib/graph/transform.ts"
      provides: "Production code passing strict-type-checked rules"
  key_links:
    - from: "packages/op-nx-polyrepo/src/lib/graph/extract.ts"
      to: "packages/op-nx-polyrepo/src/lib/graph/types.ts"
      via: "Type-only import for ExternalGraphJson"
      pattern: "import.*type.*ExternalGraphJson"
---

<objective>
Fix all production code to pass lint and typecheck under the new strict rules from Plan 01. This covers all violation categories: noUncheckedIndexedAccess guards, explicit return types, consistent-type-imports/exports, consistent-type-definitions, no-floating-promises, and any remaining strict-type-checked violations.

Purpose: Production code must be fully compliant before test files are refactored, since test factories need to match production types exactly.
Output: All 16 production .ts files passing strict lint + typecheck with zero violations.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-RESEARCH.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-01-SUMMARY.md

@packages/op-nx-polyrepo/src/lib/config/resolve.ts
@packages/op-nx-polyrepo/src/lib/graph/extract.ts
@packages/op-nx-polyrepo/src/lib/graph/types.ts
@packages/op-nx-polyrepo/src/lib/config/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autofix and resolve all production code lint violations from strict rules</name>
  <files>
    packages/op-nx-polyrepo/src/index.ts,
    packages/op-nx-polyrepo/src/lib/config/resolve.ts,
    packages/op-nx-polyrepo/src/lib/config/schema.ts,
    packages/op-nx-polyrepo/src/lib/config/validate.ts,
    packages/op-nx-polyrepo/src/lib/executors/run/executor.ts,
    packages/op-nx-polyrepo/src/lib/executors/status/executor.ts,
    packages/op-nx-polyrepo/src/lib/executors/sync/executor.ts,
    packages/op-nx-polyrepo/src/lib/format/table.ts,
    packages/op-nx-polyrepo/src/lib/git/commands.ts,
    packages/op-nx-polyrepo/src/lib/git/detect.ts,
    packages/op-nx-polyrepo/src/lib/git/normalize-url.ts,
    packages/op-nx-polyrepo/src/lib/git/patterns.ts,
    packages/op-nx-polyrepo/src/lib/graph/cache.ts,
    packages/op-nx-polyrepo/src/lib/graph/extract.ts,
    packages/op-nx-polyrepo/src/lib/graph/transform.ts,
    packages/op-nx-polyrepo/src/lib/graph/types.ts
  </files>
  <action>
Run lint and typecheck on production code, then fix ALL violations. Expected violation categories:

1. **`noUncheckedIndexedAccess`** -- Array/Record indexing now returns `T | undefined`. Add null checks or use `for...of Object.entries()` destructuring where safe. Per Pitfall 5: destructuring in for-of loops gives `T` not `T | undefined`. Prefer `if` guards over `!` assertions.

2. **`exactOptionalPropertyTypes`** (if enabled in Plan 01) -- May cause issues with @nx/devkit types. If so, Plan 01 already removed it. If enabled, fix any assignments of `undefined` to optional properties by using `delete` or conditional spreading.

3. **`@typescript-eslint/explicit-function-return-type`** -- Add explicit return types to all exported and non-exported functions in production code. Arrow functions passed as arguments and typed function expressions are exempt (allowExpressions, allowTypedFunctionExpressions).

4. **`@typescript-eslint/consistent-type-imports`** -- Change `import { Foo } from` to `import { type Foo } from` or `import type { Foo } from` for type-only imports. The autofix should handle most of these: run `npm exec nx lint @op-nx/polyrepo -- --fix` first.

5. **`@typescript-eslint/consistent-type-exports`** -- Add `type` qualifier to type-only re-exports. Autofix available.

6. **`@typescript-eslint/consistent-type-definitions`** -- Convert `type Foo = { ... }` to `interface Foo { ... }` for object shapes. Keep `type` for unions, intersections, and mapped types.

7. **`no-floating-promises`** -- Ensure all Promise returns are awaited or explicitly voided. Check executor code carefully -- it's heavily async.

8. **`no-unsafe-*` family** -- Fix any unsafe any usage (unlikely in production code since there are no explicit `any` types, but may appear from untyped library returns).

9. **`@typescript-eslint/no-non-null-assertion`** -- Replace any `!` assertions with proper null checks.

**Workflow:**
1. Run `npm exec nx lint @op-nx/polyrepo -- --fix` to autofix what ESLint can
2. Run `npm exec nx lint @op-nx/polyrepo` to see remaining violations
3. Fix remaining violations manually
4. Run `npm exec nx typecheck @op-nx/polyrepo` to check compiler violations
5. Fix typecheck violations
6. Repeat until both pass cleanly

IMPORTANT: Only fix production .ts files (NOT .spec.ts files). Test files are handled in Plans 04 and 05.

IMPORTANT: If fixing a violation requires changing a function signature or exported type, make the change -- test files will be updated in subsequent plans to match.
  </action>
  <verify>
    <automated>npm exec nx lint @op-nx/polyrepo -- --quiet 2>&1 | rg "error|warning|Error" | rg -v "spec\.ts" | head -20; npm exec nx typecheck @op-nx/polyrepo 2>&1 | tail -5</automated>
  </verify>
  <done>All 16 production .ts files pass lint (zero errors excluding test files) and typecheck with strict rules. No explicit any, no non-null assertions, all functions have return types, all imports use type qualifier where appropriate.</done>
</task>

</tasks>

<verification>
- `npm exec nx typecheck @op-nx/polyrepo` passes
- `npm exec nx lint @op-nx/polyrepo 2>&1 | rg "error" | rg -v "spec\.ts"` returns no production file errors
- `npm exec nx test @op-nx/polyrepo` -- existing tests still pass (test files may have new lint violations but should still execute)
</verification>

<success_criteria>
- Zero lint errors in production .ts files (test file violations are expected and handled in Plans 04-05)
- Zero typecheck errors
- All production functions have explicit return type annotations
- All type-only imports use `import type` or `import { type }` syntax
- Existing tests still pass (functional correctness preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-03-SUMMARY.md`
</output>
