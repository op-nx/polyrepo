---
phase: 09-cross-repo-dependency-detection
plan: '02'
type: tdd
wave: 2
depends_on:
  - 09-01
files_modified:
  - packages/op-nx-polyrepo/src/lib/graph/detect.ts
  - packages/op-nx-polyrepo/src/lib/graph/detect.spec.ts
autonomous: true
requirements:
  - DETECT-04
  - OVRD-01
  - OVRD-02
  - OVRD-03

must_haves:
  truths:
    - 'A provider-side tsconfig.base.json path alias that maps to a known project root expands the lookup map, enabling cross-repo edge detection without a package.json packageName'
    - 'Missing or unparseable tsconfig files are silently skipped (no error thrown)'
    - 'Explicit implicitDependencies entries in plugin config emit DependencyType.implicit edges for all matched project pairs (via minimatch)'
    - "A negation override ('!target') suppresses auto-detected edges from the final output, regardless of which detection path produced them"
    - 'Plugin throws at load time when any implicitDependencies override (positive or negation) references a project name pattern that matches zero projects in context.projects'
    - 'Error message includes all unknown project references in one throw — user fixes everything in one edit'
  artifacts:
    - path: 'packages/op-nx-polyrepo/src/lib/graph/detect.ts'
      provides: 'Complete detectCrossRepoDependencies — lookup map (packageName + tsconfig paths), package.json detection, override emission, negation suppression, override validation'
      exports: ['detectCrossRepoDependencies']
    - path: 'packages/op-nx-polyrepo/src/lib/graph/detect.spec.ts'
      provides: 'Unit tests for DETECT-04, OVRD-01, OVRD-02, OVRD-03 behaviors added to existing test file'
      contains: 'tsconfig path alias, override emission, negation suppression, unknown-project validation'
  key_links:
    - from: 'packages/op-nx-polyrepo/src/lib/graph/detect.ts'
      to: 'minimatch'
      via: "import { minimatch } from 'minimatch'"
      pattern: 'minimatch(name, pattern)'
    - from: 'packages/op-nx-polyrepo/src/lib/graph/detect.ts'
      to: 'packages/op-nx-polyrepo/src/lib/graph/types.ts'
      via: 'tsConfigPathsSchema Zod schema with z.object().loose()'
      pattern: 'compilerOptions.paths'
---

<objective>
Extend `detect.ts` with two additional capabilities: (1) provider-side tsconfig path alias expansion of the lookup map (DETECT-04), and (2) override processing — explicit edge emission (OVRD-01), negation suppression (OVRD-02), and unknown-project validation (OVRD-03).

Purpose: Completes the detection function. After this plan, `detectCrossRepoDependencies` handles all Phase 9 requirements. Phase 10 wires it into `createDependencies`.

Output: Extended `detect.ts` and additional tests in `detect.spec.ts` covering all four remaining requirements.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-cross-repo-dependency-detection/09-01-SUMMARY.md

@packages/op-nx-polyrepo/src/lib/graph/detect.ts
@packages/op-nx-polyrepo/src/lib/graph/detect.spec.ts
@packages/op-nx-polyrepo/src/lib/graph/types.ts
@packages/op-nx-polyrepo/src/lib/config/schema.ts
</context>

<interfaces>
<!-- Key contracts for this plan — extracted and verified from RESEARCH.md and CONTEXT.md -->

minimatch (v10.2.4 — verified named export only):

```typescript
import { minimatch } from 'minimatch';
minimatch('repo-a/my-app', 'repo-a/*'); // true
minimatch('repo-b/my-lib', 'repo-a/*'); // false
minimatch('nx/missing', 'nx/missing'); // true
```

Tsconfig Zod schema (new, to add in detect.ts):

```typescript
// Pattern: same as resolve.ts / types.ts — use .loose() not .passthrough()
const tsConfigPathsSchema = z
  .object({
    compilerOptions: z
      .object({
        paths: z.record(z.string(), z.array(z.string())).optional(),
      })
      .loose()
      .optional(),
  })
  .loose();
```

Tsconfig file path construction (avoid double-path pitfall):

```typescript
// For external repo with alias 'repo-b':
// CORRECT: join(workspaceRoot, '.repos', 'repo-b', 'tsconfig.base.json')
// WRONG:   use node.root which already has '.repos/repo-b/' prefix
```

Path alias → project root matching algorithm:

```typescript
// alias value example: ["libs/core/src/index.ts"]
// Step 1: strip filename → "libs/core/src"
// Step 2: walk up segments to find matching project root
// For each segment reduction: "libs/core/src" → "libs/core" → "libs"
// Match against: node.root.slice(`.repos/${alias}/`.length)
// i.e., strip the .repos/<alias>/ prefix from node.root for comparison
```

Override validation rule (from CONTEXT.md locked decision):

```typescript
// A pattern matches zero projects → treat as unknown → collect in unknowns[]
// Check: Object.keys(context.projects).some(name => minimatch(name, pattern))
// Strip '!' prefix before checking negation targets
// Throw once with all unknowns: `Unknown projects in implicitDependencies: ${unknowns.join(', ')}`
```

Negation suppression order (from RESEARCH.md Pitfall 4):

```typescript
// CORRECT: accumulate auto-detected edges FIRST, then filter negations as a second pass
// WRONG: skip adding edges during the detection loop when a negation exists
// Use Set<string> of "source::target" keys for O(1) negation lookup
```

PolyrepoConfig.implicitDependencies type:

```typescript
// Record<string, string[]> | undefined
// Keys: source project name patterns (minimatch globs)
// Values: target project name patterns (minimatch globs), '!' prefix = negation
```

</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: tsconfig path alias expansion — provider-side lookup map enrichment (DETECT-04)</name>
  <files>packages/op-nx-polyrepo/src/lib/graph/detect.ts, packages/op-nx-polyrepo/src/lib/graph/detect.spec.ts</files>

  <behavior>
    RED phase — add these failing tests to detect.spec.ts:

    Tsconfig path alias → lookup map expansion:
    - Test: external repo-b has `tsconfig.base.json` at `.repos/repo-b/tsconfig.base.json` with `compilerOptions.paths["@acme/core"] = ["libs/core/src/index.ts"]`, and repo-b has a TransformedNode with root `.repos/repo-b/libs/core`. Result: lookup map gains `'@acme/core' -> 'repo-b/core'`. When a consumer depends on `@acme/core`, a cross-repo edge is emitted.
    - Test: packageName takes precedence — if TransformedNode already has `packageName: '@acme/core'` AND the tsconfig also declares `@acme/core`, the map entry from `packageName` wins (inserted first, tsconfig alias skips due to `Map.has()` guard)
    - Test: tsconfig path alias values with multiple array entries — only the first entry is used for root matching (or all entries are checked; implementation has discretion — but at least one match is found)

    Fallback from tsconfig.base.json to tsconfig.json:
    - Test: repo with no `tsconfig.base.json` but with `tsconfig.json` containing path aliases — the fallback file is read and aliases are expanded into the lookup map
    - Test: repo with neither tsconfig.base.json nor tsconfig.json — silently skipped, no error thrown

    Path alias root matching edge cases:
    - Test: alias value `["libs/core/src/index.ts"]` → strips filename → `libs/core/src` → walks up until matching project root `libs/core` → maps to `repo-b/core`
    - Test: alias value `["libs/core/src/index.ts"]` where no TransformedNode root matches any prefix → alias is silently ignored (no lookup map entry added, no error)

    Host workspace tsconfig:
    - Test: host workspace `tsconfig.base.json` (at `workspaceRoot/tsconfig.base.json`) with `paths["@host/utils"] = ["libs/utils/src/index.ts"]` where host has a project rooted at `libs/utils` — lookup map gains `'@host/utils' -> '<host-project-name>'`

    GREEN phase — extend detect.ts lookup map construction:

    After inserting packageNames (from external nodes and host projects), add a tsconfig expansion step:

    1. For each external repo (keyed by alias in report.repos):
       - Try to read `join(workspaceRoot, '.repos', alias, 'tsconfig.base.json')` (AVOID using `node.root` — that already includes `.repos/<alias>/`)
       - If not found, try `join(workspaceRoot, '.repos', alias, 'tsconfig.json')`
       - Use silent try/catch (same pattern as transform.ts)
       - Parse via `tsConfigPathsSchema.safeParse()`. If `!result.success`, silently skip.
       - For each path alias key and its values array:
         - Take the first value (or iterate all until a match is found)
         - Strip filename: `value.slice(0, value.lastIndexOf('/'))`
         - Walk up path segments until a match: compare against TransformedNode roots (stripping `.repos/<alias>/` prefix from `node.root`)
         - If match found AND alias key not already in map: `map.set(aliasKey, node.name)`

    2. For host workspace tsconfig:
       - Try `join(workspaceRoot, 'tsconfig.base.json')`, fallback to `join(workspaceRoot, 'tsconfig.json')`
       - Same Zod parse + silent-skip pattern
       - For each path alias key and its values array:
         - Strip filename, walk up path segments
         - Compare against `context.projects[name].root` (already relative to workspaceRoot)
         - If match found AND alias key not already in map: `map.set(aliasKey, projectName)`

    3. Add `tsConfigPathsSchema` as a module-level Zod schema in `detect.ts`. Import `z` from `'zod'`.

    Type safety: all `readFileSync` calls return `string` — narrow with `typeof raw === 'string'` before `JSON.parse`. Or call `String(result)` — no cast.

  </behavior>

  <action>
    RED: Add tsconfig tests to detect.spec.ts. Tests must reference tsconfig mock responses via `vi.mocked(readFileSync).mockImplementation(...)`. Run tests — new tests must fail. Commit: `test(09-02): add failing tests for tsconfig path alias lookup map expansion`

    GREEN: Extend lookup map construction in detect.ts with tsconfig reading logic. Run all tests — must pass. Commit: `feat(09-02): expand lookup map with provider-side tsconfig path aliases`

  </action>

  <verify>
    <automated>pnpm nx test @op-nx/polyrepo --output-style=static</automated>
  </verify>

  <done>
    - Tsconfig path alias tests added and passing
    - Provider-side tsconfig reading expands the lookup map
    - Silent-skip behavior verified for missing/invalid tsconfigfiles
    - packageName precedence over tsconfig aliases verified
    - All existing tests still pass
  </done>
</task>

<task type="tdd">
  <name>Task 2: Override emission, negation suppression, and unknown-project validation (OVRD-01, OVRD-02, OVRD-03)</name>
  <files>packages/op-nx-polyrepo/src/lib/graph/detect.ts, packages/op-nx-polyrepo/src/lib/graph/detect.spec.ts</files>

  <behavior>
    RED phase — add these failing tests to detect.spec.ts:

    OVRD-01 — explicit override edge emission:
    - Test: config with `implicitDependencies: { 'repo-a/my-app': ['repo-b/my-lib'] }` emits one `DependencyType.implicit` edge `{ source: 'repo-a/my-app', target: 'repo-b/my-lib', type: 'implicit' }` (no `sourceFile` field)
    - Test: config with glob key `implicitDependencies: { 'repo-a/*': ['repo-b/my-lib'] }` where `context.projects` contains `repo-a/app1` and `repo-a/app2` — emits two implicit edges, one per matched source
    - Test: config with glob target `implicitDependencies: { 'repo-a/my-app': ['repo-b/*'] }` where `context.projects` contains `repo-b/lib1` and `repo-b/lib2` — emits two implicit edges, one per matched target

    OVRD-02 — negation suppression:
    - Test: config with `implicitDependencies: { 'repo-a/my-app': ['!repo-b/my-lib'] }` where auto-detection would emit an edge `repo-a/my-app → repo-b/my-lib` — the negation suppresses that edge from the output (zero edges emitted)
    - Test: negation only removes the specific source+target pair; other auto-detected edges from the same source to different targets are NOT suppressed
    - Test: negation applies to edges detected via package.json AND edges that would be detected via tsconfig alias — source of detection does not affect suppression

    OVRD-03 — unknown project validation:
    - Test: config with `implicitDependencies: { 'nx/missing-lib': ['repo-b/my-lib'] }` where `'nx/missing-lib'` matches zero projects in `context.projects` — throws `Error` with message containing `'Unknown projects in implicitDependencies: nx/missing-lib'`
    - Test: config with `implicitDependencies: { 'repo-a/my-app': ['repo-b/ghost'] }` where `'repo-b/ghost'` matches zero projects — throws with message containing `'Unknown projects in implicitDependencies: repo-b/ghost'`
    - Test: config with both an unknown key AND an unknown target — throws once with BOTH names in the error message (comma-separated)
    - Test: config with negation override `'!repo-b/ghost'` where `'repo-b/ghost'` matches zero projects — also throws (negation targets follow same strict validation as positive targets)
    - Test: config with no `implicitDependencies` field (undefined) — no error thrown, proceeds normally

    Correct execution order (negation after accumulation):
    - Test verifying that negation is applied as a post-filter: if `implicitDependencies` has BOTH `'repo-b/my-lib'` (positive) and `'!repo-b/my-lib'` (negation) targeting the same project, the negation wins — zero edges for that pair

    GREEN phase — extend detect.ts with override processing:

    Add to `detectCrossRepoDependencies` after lookup map construction and auto-detection:

    1. **Override validation (OVRD-03)** — runs FIRST, before emitting any edges:
       - If `config.implicitDependencies` is undefined, skip.
       - Collect unknowns: for each key pattern in implicitDependencies, check `Object.keys(context.projects).some(name => minimatch(name, key))`. If false, add key to `unknowns[]`.
       - For each target string in each value array: strip `!` prefix if present, then check same way. If false, add stripped target to `unknowns[]`.
       - If `unknowns.length > 0`: throw `new Error(\`Unknown projects in implicitDependencies: ${unknowns.join(', ')}\`)`

    2. **Negation set construction** — build `Set<string>` of `"source::target"` keys to suppress:
       - For each key pattern, find all matching source projects (`Object.keys(context.projects).filter(name => minimatch(name, keyPattern))`).
       - For each value string starting with `!`: strip prefix, find all matching target projects, add `"source::target"` to negation set for every source+target pair.

    3. **Auto-detection result filtering** — remove suppressed edges:
       - After accumulating auto-detected edges (from Plan 01's dep-list scan + tsconfig expansion edges), filter out any edge whose `"source::target"` key is in the negation set.

    4. **Override edge emission (OVRD-01)** — add implicit edges:
       - For each key pattern, find all matching source projects.
       - For each positive value string (no `!` prefix), find all matching target projects.
       - For each source+target pair, emit `{ source, target, type: DependencyType.implicit }` (no sourceFile).
       - Deduplicate with the existing `Set<string>` keyed by `"source::target"` — skip if already present (override dedup is separate from auto-detection dedup).

    Import to add: `import { minimatch } from 'minimatch';`

    Execution order in detectCrossRepoDependencies:
    1. Build lookup map (packageNames → external nodes → host projects → tsconfig aliases)
    2. Run override validation (throws if any pattern matches zero projects)
    3. Accumulate auto-detected edges (dep-list scan for all external + host nodes)
    4. Build negation set from override entries
    5. Filter auto-detected edges using negation set
    6. Emit override edges (positive only)
    7. Return combined array: filtered auto-detected edges + override edges

  </behavior>

  <action>
    RED: Add override tests to detect.spec.ts. Tests must fail (detectCrossRepoDependencies does not yet handle overrides). Commit: `test(09-02): add failing tests for override emission, negation, and unknown-project validation`

    GREEN: Extend detect.ts with validation + negation set + auto-detection filter + override emission. Run all tests — must pass. Commit: `feat(09-02): implement override processing and negation suppression`

    REFACTOR (if needed): Clean up, extract helpers, verify no `as` casts. Commit: `refactor(09-02): clean up override processing logic`

    Final gate: `pnpm nx build @op-nx/polyrepo --output-style=static` must compile clean.

  </action>

  <verify>
    <automated>pnpm nx test @op-nx/polyrepo --output-style=static</automated>
  </verify>

  <done>
    - OVRD-01: explicit `implicitDependencies` overrides emit `DependencyType.implicit` edges for matched project pairs
    - OVRD-02: `!target` negation suppresses auto-detected edges (applied as post-filter)
    - OVRD-03: unknown project patterns throw with all unknowns in a single error; negation targets follow same validation
    - Glob patterns (both key and value) matched via `minimatch` (named import)
    - No `as` casts, no `any`, no `!` non-null assertions, no hooks in test file
    - All tests pass (290+ existing + all new Phase 9 tests)
    - TypeScript build compiles clean: `pnpm nx build @op-nx/polyrepo --output-style=static`
    - `detect.ts` is a complete, pure function ready to be wired in Phase 10
  </done>
</task>

</tasks>

<verification>
1. `pnpm nx test @op-nx/polyrepo --output-style=static` — all tests pass
2. `pnpm nx build @op-nx/polyrepo --output-style=static` — TypeScript compiles clean
3. `detectCrossRepoDependencies` handles all 7 requirements: DETECT-01 through DETECT-04, OVRD-01 through OVRD-03
4. detect.ts has zero top-level side effects (pure function module)
5. detect.spec.ts uses SIFERS pattern (no hooks), vi.mocked() (no casts)
</verification>

<success_criteria>

- DETECT-04: tsconfig path alias from provider repos expands lookup map and enables cross-repo edge detection
- OVRD-01: explicit implicitDependencies config entries emit implicit edges for matched project pairs
- OVRD-02: '!target' negation suppresses auto-detected edges from the final output
- OVRD-03: unknown project references in implicitDependencies throw once with all unknowns listed
- All existing tests (290+) continue to pass
- TypeScript build is clean
- `detect.ts` is ready to be imported and wired by Phase 10
  </success_criteria>

<output>
After completion, create `.planning/phases/09-cross-repo-dependency-detection/09-02-SUMMARY.md`
</output>
