---
phase: 05-avoid-type-casting-and-prefer-satisfies
plan: 06
type: execute
wave: 3
depends_on: ['05-02', '05-03', '05-04', '05-05']
files_modified:
  - eslint.config.mjs
  - .claude/skills/type-safety/SKILL.md
  - .claude/skills/type-safety/rules/satisfies-patterns.md
  - .claude/skills/type-safety/rules/zod-validation.md
  - .claude/skills/type-safety/rules/typed-mocks.md
  - .claude/skills/type-safety/rules/sifer-pattern.md
autonomous: false
requirements:
  - SAFE-ENFORCE
  - SAFE-SKILLS

must_haves:
  truths:
    - 'Zero eslint-disable comments in the entire codebase'
    - 'Zero beforeEach/afterEach hooks in any test file'
    - 'Full lint + typecheck + test suite passes with zero errors'
    - 'Project-local skills teach AI agents how to resolve ESLint bans'
    - 'Enforcement mechanism prevents regression'
  artifacts:
    - path: '.claude/skills/type-safety/SKILL.md'
      provides: 'Skill index for type safety patterns'
    - path: '.claude/skills/type-safety/rules/satisfies-patterns.md'
      provides: 'When and how to use satisfies vs type annotation'
    - path: '.claude/skills/type-safety/rules/zod-validation.md'
      provides: 'Zod safeParse at system boundaries'
    - path: '.claude/skills/type-safety/rules/typed-mocks.md'
      provides: 'Cast-free Vitest mock patterns'
    - path: '.claude/skills/type-safety/rules/sifer-pattern.md'
      provides: 'SIFER test setup pattern'
  key_links:
    - from: '.claude/skills/type-safety/SKILL.md'
      to: 'eslint.config.mjs'
      via: 'Skills teach alternatives for what ESLint bans'
      pattern: 'eslint'
---

<objective>
Final enforcement verification and skills creation. Verify the entire codebase has zero eslint-disable comments, zero lint/typecheck errors, and all tests pass. Then create project-local Claude skills that teach AI agents how to work within the strict type safety constraints.

Purpose: Close the loop -- verify all violations are resolved, then create teaching material so future AI-assisted development maintains the standard. Per user decision: "ESLint enforces bans, skills teach alternatives."
Output: Verified clean codebase + 4 skill rule files + enforcement hook.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-RESEARCH.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-01-SUMMARY.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-02-SUMMARY.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-03-SUMMARY.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-04-SUMMARY.md
@.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-05-SUMMARY.md

@eslint.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final enforcement verification -- zero violations across entire codebase</name>
  <files>eslint.config.mjs</files>
  <action>
Run the full verification suite and fix any remaining issues:

1. **Zero eslint-disable comments:**

   ```
   git grep "eslint-disable" -- "packages/op-nx-polyrepo/src/"
   ```

   Must return empty. If any remain, fix them using the patterns established in Plans 04-05.

2. **Zero beforeEach/afterEach hooks:**

   ```
   git grep "beforeEach\|afterEach\|beforeAll\|afterAll" -- "packages/op-nx-polyrepo/src/**/*.spec.ts"
   ```

   Must return empty (only `import { beforeEach }` lines in imports are OK -- but those should be removed too since they're unused). If any remain, refactor to SIFER.

3. **Full lint pass:**

   ```
   npm exec nx lint @op-nx/polyrepo
   ```

   Must pass with zero errors AND zero warnings. If vitest/no-hooks violations remain (because hooks weren't fully removed), fix them.

4. **Full typecheck pass:**

   ```
   npm exec nx typecheck @op-nx/polyrepo
   ```

   Must pass with zero errors.

5. **Full test pass:**

   ```
   npm exec nx test @op-nx/polyrepo
   ```

   Must pass with all tests green.

6. **Cross-project verification:**
   ```
   npm exec nx run-many --targets=test,lint,typecheck
   ```
   Must pass for all projects in the workspace.

If any issues are found, fix them. The eslint.config.mjs may need minor adjustments if vitest/no-hooks or other rules have edge cases that weren't caught in earlier plans.

Document the final violation count: should be ZERO across lint, typecheck, and test.
</action>
<verify>
<automated>npm exec nx run-many --targets=test,lint,typecheck 2>&amp;1 | tail -10</automated>
</verify>
<done>Zero eslint-disable comments. Zero lint errors. Zero typecheck errors. All tests pass. Full run-many passes across workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Create project-local type safety skills for AI agents</name>
  <files>
    .claude/skills/type-safety/SKILL.md,
    .claude/skills/type-safety/rules/satisfies-patterns.md,
    .claude/skills/type-safety/rules/zod-validation.md,
    .claude/skills/type-safety/rules/typed-mocks.md,
    .claude/skills/type-safety/rules/sifer-pattern.md
  </files>
  <action>
Create project-local skills that teach AI agents HOW to work within the strict type safety constraints. Per user decision: "ESLint enforces bans, skills teach alternatives" and "Do NOT add type safety rules to AGENTS.md."

**Create `.claude/skills/type-safety/SKILL.md`** (~130 lines, lightweight index):

- Skill name: "Type Safety Patterns"
- Trigger: When AI agent encounters ESLint errors for banned patterns (as assertions, any, non-null assertions, test hooks)
- Overview: This project enforces maximum type safety via ESLint strict-type-checked + custom rules. This skill teaches the approved alternatives.
- Rule index with one-line descriptions pointing to each rules/\*.md file
- Quick reference: banned pattern -> approved alternative table

**Create `.claude/skills/type-safety/rules/satisfies-patterns.md`**:

- Decision tree: when to use `satisfies` vs type annotation vs `as const satisfies`
- Per user decisions: return type annotation for exported functions, `satisfies` when narrow type matters
- Examples from the actual codebase (config objects, lookup tables)
- Excess property checking behavior explanation
- `as const` usage (permitted via `allowAsConst: true` in ESLint config)

**Create `.claude/skills/type-safety/rules/zod-validation.md`**:

- Pattern: Zod safeParse at ALL JSON.parse boundaries
- When to use: Any external data (JSON.parse, API responses, file reads)
- How to create schemas: match the interface, use z.infer for type derivation
- Examples from the actual codebase (resolve.ts, extract.ts, executor.ts patterns)
- Prefer z.safeParse() over hand-written type guards

**Create `.claude/skills/type-safety/rules/typed-mocks.md`**:

- How to mock overloaded functions without casts (Vitest 4.x vi.mocked pattern)
- How to create typed test factories (ExecutorContext, graph nodes, ChildProcess)
- Ban: no `as` casts in test files, no eslint-disable comments
- Examples from the actual codebase (the patterns established in Plans 04-05)

**Create `.claude/skills/type-safety/rules/sifer-pattern.md`**:

- What SIFERs are (link to Kolodny article)
- Pattern: setup() function replaces beforeEach
- vi.clearAllMocks() inside setup, not in hooks
- Return all mutable state from setup
- Injectable parameters for per-test customization
- Examples from the actual codebase
- Ban: vitest/no-hooks enforced -- no beforeEach, afterEach, beforeAll, afterAll

Each rule file should be 50-100 lines. Include actual code examples from the codebase (not generic examples). Reference specific files where patterns are demonstrated.

Do NOT modify AGENTS.md or CLAUDE.md with type safety rules -- the skills are the teaching mechanism.
</action>
<verify>
<automated>ls .claude/skills/type-safety/SKILL.md .claude/skills/type-safety/rules/satisfies-patterns.md .claude/skills/type-safety/rules/zod-validation.md .claude/skills/type-safety/rules/typed-mocks.md .claude/skills/type-safety/rules/sifer-pattern.md &amp;&amp; echo "[OK] all skill files exist"</automated>
</verify>
<done>5 skill files created. SKILL.md indexes 4 rule files. Each rule teaches the approved alternative to a banned pattern with codebase-specific examples. No changes to AGENTS.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete type safety hardening</name>
  <files>N/A</files>
  <action>
Human verifies the complete type safety hardening:

1. Run `npm exec nx run-many --targets=test,lint,typecheck` -- should pass with zero errors
2. Run `git grep "eslint-disable" -- packages/op-nx-polyrepo/src/` -- should return empty
3. Run `git grep "beforeEach" -- packages/op-nx-polyrepo/src/` -- should return empty
4. Review `.claude/skills/type-safety/SKILL.md` -- should index 4 rule files with clear descriptions
5. Optionally: introduce an `as` cast in any file, run lint -- should error immediately
   </action>
   <verify>Human approval</verify>
   <done>Human confirms: zero violations, skills are useful, enforcement works.</done>
   </task>

</tasks>

<verification>
- `npm exec nx run-many --targets=test,lint,typecheck` passes
- `git grep "eslint-disable" -- packages/op-nx-polyrepo/src/` returns empty
- `git grep "beforeEach\|afterEach" -- packages/op-nx-polyrepo/src/**/*.spec.ts` returns empty
- `.claude/skills/type-safety/SKILL.md` exists and indexes 4 rule files
- All 4 rule files exist in `.claude/skills/type-safety/rules/`
</verification>

<success_criteria>

- Zero eslint-disable comments across entire source tree
- Zero lint errors, zero typecheck errors, all tests pass
- 5 skill files created teaching approved patterns
- Human verifies the final state
  </success_criteria>

<output>
After completion, create `.planning/phases/05-avoid-type-casting-and-prefer-satisfies/05-06-SUMMARY.md`
</output>
